Requires Python 3 and Pygame library to run.
Install Python version 3.7 at https://www.python.org/downloads/
Once installed open a command prompt and install Pygame:
    pip install pygame
    press enter
Now you should be able to run the script from the command prompt:
    python open\file\location\pytetris.py
